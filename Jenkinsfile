pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "HelloWorld"
               // sh 'javac HelloWorld.java'
            }
        }
        stage('Run') {
            steps {
                echo "HelloWorld"
                //sh 'java HelloWorld'
            }
        }
    }
}
